__pycache__/
*.pyc
.pytest_cache/

# Keys and tokens. This repository is public: only .env.example is tracked.
.env
.env.*
!.env.example

# companies.txt backups from `careers_bot.py resolve --write` (the file itself is tracked)
*.txt.bak

# build_companies.py probe results (local cache, ~3 MB)
job-hunt/assets/probe_cache.json
